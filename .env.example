# Asterisk PJSIP Configuration
ASTERISK_HOST=192.168.1.100
ASTERISK_PORT=5060
ASTERISK_USER=sip
ASTERISK_PASSWORD=sip123
ASTERISK_REALM=asterisk.local
ASTERISK_CONTEXT=from-whatsapp

# SIP Transport (UDP, TCP, or TLS)
SIP_TRANSPORT=UDP

# RTP Configuration
RTP_PORT_MIN=10000
RTP_PORT_MAX=20000

# Logging
LOG_LEVEL=debug
LOG_FILE=./logs/gateway.log

# WhatsApp
WHATSAPP_SESSION_DIR=./auth_info

# Network
BIND_ADDRESS=0.0.0.0
